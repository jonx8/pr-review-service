# PR Review Service

Микросервис для автоматического назначения ревьюеров на Pull Request'ы внутри команд.

## 📋 Описание

Сервис предоставляет HTTP API для:
- Автоматического назначения ревьюеров из команды при создании Pull Request'а
- Управления командами и пользователями
- Переназначения ревьюеров
- Получения списка PR по ревьюверу

## 🛠 Стек технологий
- Go 1.25.3
- PostgreSQL 15
- Gin
- SQLx
- go-migrate
- Docker & Docker Compose

## 🚀 Быстрый старт

### Вариант 1: Запуск через Docker

#### Предварительные требования
- Docker
- Docker Compose

#### Запуск

```bash
# С помощью Make
make docker-up

# Или напрямую через Docker Compose
docker compose up --build -d
```

Можно проверить запуск сервиса, перейдя по адресу: http://localhost:8080/health

#### Остановка
``` bash
# Отключить сервисы Docker Compose
make docker-down
```

#### Просмотр логов
```bash
make docker-logs
```

#### Полная очистка (включая данные БД)
```bash
make clean
```

### Вариант 2: Локальный запуск без Docker
#### Предварительные требования
- Go 1.25+
- PostgreSQL 15+

#### Запуск приложения
##### 1. Установка зависимостей

``` bash
# Установка зависимостей
go mod download
```

##### 2. Настройка базы данных
Запустите PostgreSQL и создайте базу данных для приложения, например:

```sql
CREATE DATABASE pr_review;
CREATE USER postgres WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE pr_review TO postgres;
```

##### 3. Установка переменных окружения
Выставите переменные окружения в соответствии с примером в файле [.env.example](https://github.com/jonx8/pr-review-service/blob/master/.env.example).

```bash
export DB_HOST=localhost
export DB_PORT=5432
export DB_USER=postgres
export DB_PASSWORD=your_password
export DB_NAME=pr_review
export ENVIRONMENT=development
```

##### 4. Запуск
```bash
# Запуск сервиса
make run

# Или сборка и запуск
make build
./bin/app
```

## Структура проекта
```bash
.
├── api/                            # OpenAPI спецификации
├── cmd/
│   └── server/
│       └── main.go                 # Точка входа приложения
├── internal/
│   ├── app/                        # Инициализация приложения
│   ├── config/                     # Конфигурация приложения
│   ├── database/                   # Работа с базой данных
│   ├── handlers/                   # HTTP обработчики (Gin)
│   ├── services/                   # Бизнес-логика
│   ├── repositories/               # Слой данных (SQLx)
│   ├── models/                     # Структуры данных
│   ├── errors/                     # Кастомные ошибки
│   └── utils/                      # Вспомогательные утилиты
├── migrations/                     # SQL миграции (go-migrate)
├── docker-compose.yml              # Конфигурация Docker Compose
├── Dockerfile                      # Конфигурация Docker
├── Makefile                        # Автоматизация задач
├── .env.example                    # Шаблон переменных окружения
└── go.mod                          # Зависимости Go
```